/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your config options here
  // Example: reactStrictMode: true,

  // If you were trying to enable React Compiler (available experimentally in Next 14):
  experimental: {
    reactCompiler: true,
  },
};

module.exports = nextConfig;